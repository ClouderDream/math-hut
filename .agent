# 云梦的数理小屋 —— Agent 工作约定

## 项目信息
- 仓库：`ClouderDream/math-hut`
- 站点：`https://clouderDream.github.io/math-hut/`
- 当前活跃版本：`v1.1/`
- 历史版本：`v1.0/`（保留，不删除）

## 目录约定
- `v1.1/`：当前可执行的博客完整代码（src/content/docs/scripts/dist/node_modules/package.json/site.config.js 等）。
- `v1.1/tmp/`：中间产物、截图、临时文件。
- `tools/`：长期复用工具与说明。
- `.github/workflows/deploy.yml`：必须留在仓库根，GitHub Actions 只认根目录的 `.github/workflows/`。

## 构建与部署
- 构建：`cd v1.1 && node src/build.js`（产物到 `v1.1/dist/`）
- 预览：`cd v1.1 && node scripts/dev.js --port 4173`
- 部署：`git push origin main` → Actions 从 `v1.1/` 构建并部署到 Pages。
- 凭据：GCM `manager` 已写入 Windows 凭据管理器；非交互 push 若失败，用 CDP 浏览器新建 Classic PAT（`repo`+`workflow`）临时推送并 `git credential approve`。

## 后台管理
- 地址：`https://clouderDream.github.io/math-hut/admin/`
- 管理口令：`cloudream666`
- GitHub Token：PAT with `repo` scope（Classic 或 Fine-grained Contents Read/Write）
- 后台功能：文章新建/编辑/删除、标签选择/新增/删除、图片上传、站点文字设置、顶部封面图设置。

## 关键文件
- 全站配置：`v1.1/site.config.js` + `v1.1/content/site.json`（后者由后台编辑）
- 文章：`v1.1/content/posts/`
- 页面：`v1.1/content/pages/`
- 样式：`v1.1/src/assets/css/main.css`
- 后台模板：`v1.1/src/templates/admin.ejs`
- 后台逻辑：`v1.1/src/admin/admin.js`、`v1.1/src/admin/github.js`

## 版本迭代规则
- 小改动：在活跃版本 `v1.1/` 内迭代，保留旧 `v1.0/`。
- 大改动或确认稳定的下一个阶段：复制 `v1.1/` → `v1.2/` 或 `v2.0/`，改 `deploy.yml` 与 `.agent` 指向新版本。
- 永远不要在仓库根直接放博客源文件（根只放 `.github/`、版本文件夹、`tools/`、README、`.agent`）。
