# 云梦的数理小屋 —— Agent 工作约定

## 项目信息
- 仓库：`ClouderDream/math-hut`
- 站点：`https://clouderdream.github.io/math-hut/`
- 当前活跃版本：`v1.4/`
- 历史稳定版本：`v1.0/` ~ `v1.3/`（保留，不删除）
- 跨版本本地工具：`tools/local-ocr/`

## 目录约定
- `v1.4/`：当前可执行博客完整代码、内容、后台与构建系统。
- `v1.0/` ~ `v1.3/`：历史版本，只用于追溯与回滚。
- `tools/local-ocr/`：Windows 本机 OCR / 草图服务，模型与虚拟环境不得提交。
- `.github/workflows/deploy.yml`：GitHub Pages 构建入口。
- `VERSION_HISTORY.md`：重要改动、Bug、修复与长期设计原则。

## 构建与部署
- 构建：`cd v1.4 && npm ci && npm run build`
- 冒烟检查：`cd v1.4 && npm run test:smoke`
- 预览：`cd v1.4 && npm run preview`
- 部署：推送 `main` 后 Actions 从 `v1.4/` 构建并部署 Pages。

## 后台管理
- 地址：`https://clouderdream.github.io/math-hut/admin/`
- 后台通过 GitHub API 直接管理 `v1.4/content/`。
- 管理口令、GitHub PAT、Cookie、Token 等凭据不得写入仓库、文档、日志或 Agent 文件。
- GitHub PAT 优先使用 Fine-grained Token，仅授权本仓库 Contents Read/Write。

## Local OCR
- 服务：`http://127.0.0.1:8765`
- 实现：PaddleOCR PP-StructureV3，PaddlePaddle CPU 基线 3.2.2。
- 长任务使用 `POST /ocr/start` + `GET /ocr/status/{job_id}`。
- 草图 V1.4 流程：浏览器框选真实区域 → 返回裁切图 → 复杂度足够低才生成 SVG/TikZ。
- 服务默认只允许监听 loopback，不改成 `0.0.0.0`。
- OCR 结果只作为初稿，数学符号必须人工校对。

## V1.4 OCR UI 原则
- 上传原稿区比校对区窄，左右卡片顶部和底部对齐。
- Markdown 与实时预览桌面端同时显示，并按相对阅读进度同步滚动。
- 草图处理首先展示真实裁切结果；复杂选区不得输出误导性的伪 SVG/TikZ。
- 小屏自动改为单列，不能依赖固定大屏宽度。

## 关键文件
- 全站配置：`v1.4/site.config.js` + `v1.4/content/site.json`
- 文章：`v1.4/content/posts/`
- 页面：`v1.4/content/pages/`
- 样式：`v1.4/src/assets/css/`
- 后台模板：`v1.4/src/templates/admin.ejs`
- OCR 后台：`v1.4/src/admin/ocr-workspace.js`
- OCR 服务：`tools/local-ocr/server.py`

## 版本迭代规则
- 小改动：在当前活跃版本 `v1.4/` 内迭代。
- 大改动：复制当前稳定版本到下一个目录，再切换 deploy/workflow。
- 每次重大变更前创建回滚分支。
- 不删除历史稳定版本。
