# 构建并部署到 GitHub Pages
#
# 触发：推送到 main 分支，或在 Actions 页面手动点 "Run workflow"
name: Build & Deploy Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

# 站点只需要部署最新 main；新提交会取消仍在排队/运行的旧部署，避免后台连续保存或自动简报造成队列堆积。
concurrency:
  group: pages
  cancel-in-progress: true

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: v1.3
    steps:
      - name: 拉取代码
        uses: actions/checkout@v4

      - name: 准备 Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: v1.3/package-lock.json

      - name: 安装依赖
        run: npm ci

      - name: 构建静态站点
        run: npm run build

      - name: 全站冒烟检查
        run: npm run test:smoke

      - name: 生成 .nojekyll（否则 Pages 会忽略下划线开头的目录）
        run: touch dist/.nojekyll

      - name: 配置 Pages
        uses: actions/configure-pages@v5

      - name: 上传产物
        uses: actions/upload-pages-artifact@v3
        with:
          path: v1.3/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: 部署到 GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
