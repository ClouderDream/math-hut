# 首次运行提示

首次运行 OCR 时，PaddleOCR 可能下载模型并进行初始化，因此耗时、CPU、内存和磁盘占用都会显著高于后续调用。

建议：

1. 先运行 `doctor.cmd`；
2. 再运行 `start.cmd`；
3. 第一次 OCR 用一张较小、清晰的图片验证；
4. 模型下载完成后再测试整页手写笔记或扫描 PDF；
5. 若内存不足，不要同时在浏览器加载超大 PDF 和多个 OCR 任务。

本地服务只监听 `127.0.0.1`，关闭命令为 `stop.cmd`。
